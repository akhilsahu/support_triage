# Core Framework
fastapi>=0.109.0
uvicorn[standard]==0.27.0
sse-starlette>=1.6.1

# Database
sqlalchemy>=2.0.31
asyncpg>=0.30.0
psycopg2-binary>=2.9.10
pgvector==0.2.4
alembic==1.13.1
greenlet==3.2.5

# Data Validation
pydantic>=2.8.0
pydantic-settings>=2.1.0

# Caching
redis==5.0.1
aioredis==2.0.1

# AI & ML
chromadb>=0.6.0
pypdf==4.3.1
pymupdf>=1.24.0
python-docx==1.1.2
beautifulsoup4==4.12.3
lxml==5.2.2
openai>=1.90.0
anthropic>=0.34.0
mem0ai>=0.1.0
agno>=1.5.0
ibm-watsonx-ai>=1.0.0

# Document Ingestion — extended format support
openpyxl>=3.1.0          # .xlsx parsing (XlsxParser)
xlrd>=2.0.1              # .xls legacy Excel conversion
python-pptx>=1.0.0       # .pptx / slide extraction (PptxParser)
markdown-it-py>=3.0.0    # Markdown heading-aware parsing (TextParser)

# Retrieval reranking (optional; off unless RERANK_ENABLED=true)
cohere>=5.0.0            # RERANK_PROVIDER=cohere (hosted; needs COHERE_API_KEY)
# sentence-transformers>=3.0.0   # RERANK_PROVIDER=sentence_transformer (LOCAL, no key,
#                                # but pulls in PyTorch ~1.5GB — install only if you
#                                # deliberately want the local reranker)

# Agno session store (history / user-memory / summaries) uses PostgresDb via
# SQLAlchemy + psycopg2-binary (already pinned above). No extra package needed.

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Utilities
python-multipart==0.0.6
python-dotenv==1.0.0
httpx==0.27.2
websockets==12.0
tenacity==8.2.3

# Monitoring & Logging
prometheus-client==0.19.0
structlog==24.1.0
email-validator
